# InboxFly 开源版测试清单

> 环境：本地 `npm run dev` → http://localhost:8788 · 初始账号 `admin` / `inboxfly123`（或重走向导）
> 标记：✅ 已由自动化/脚本回归 · ☐ 待手动测试 · 🌐 需真实环境（本地无法测试）

---

## A. 核心管道（已自动化回归 ✅，可抽查）

- [x] 首次初始化：SETUP_TOKEN 校验（错误 token 403）→ 创建管理员 → 会话签发
- [x] 登录：正确/错误密码、限流（同 IP 每分钟 10 次 → 429）
- [x] 模拟收信走生产同款管道：4 封中黑名单邮件被正确拦截且记录原因 `from_blacklist`
- [x] 规则匹配优先级：exact > catchall、先匹配先停
- [x] 筛选器语法：完整地址 / @域名后缀 / /正则/ 三种黑名单写法
- [x] 批量删除：删除 2 封（R2 对象 + D1 级联清理）
- [x] R2 清理：`days=0` 删除全部对象、`days=365` 删除 0
- [x] Turnstile：无密钥开启 422、无 token 登录 403、测试密钥全链路通过
- [x] 安全边界：无 CSRF 403、未登录 401、未验证目标地址 422

## B. 本地手动测试（建议按顺序过一遍）

### B1 登录与向导
- [ ] 退出登录 → 登录页新设计（logo/标题/密码眼睛/保持登录/Turnstile 组件）
- [ ] 密码眼睛按钮切换明文
- [ ] 勾选/不勾选「保持登录」分别登录（Cookie 有效期 30 天 / 24 小时）
- [ ] 重置环境（`rm -rf .wrangler` + 重启）走完整五步向导：每步原因/风险文案 → 目标地址验证联动 → 首条规则自动生成 → default_forward 自动设置

### B2 邮件
- [ ] 模拟收信 ×5 / ×20
- [ ] 域名 chip 筛选、状态筛选、关键词搜索（发件人/主题/正文）
- [ ] HTML 邮件：沙箱渲染正常、远程图片显示「已屏蔽」占位、cid 内嵌图走鉴权接口
- [ ] 附件下载（invoice.pdf / build-log.txt / bounce-log.txt）
- [ ] 原始 MIME（.eml）下载后可用邮件客户端打开
- [ ] 纯文本邮件：正文直接展示（不要求下载 .eml）
- [ ] 单封删除 + 批量勾选删除（按钮出现 → 确认 → 列表刷新）
- [ ] 拦截邮件默认只显示元数据（在设置开启「存储被拦截邮件正文」后重模拟对比）

### B3 规则
- [ ] 新建 catchall 转发规则（目标地址仅显示已验证）
- [ ] 新建 exact 规则（精确地址）
- [ ] 三种动作各建一条：forward / block（退信）/ blackhole（丢弃）——模拟收信后核对状态徽章
- [ ] 筛选器：黑名单多行、主题正则（填一个非法正则看 422）、大小上限（>25 看 422）、附件条件
- [ ] 拖拽排序 ↑↓（模拟收信验证顺序生效）
- [ ] 启停开关（停用后模拟收信不再命中）

### B4 目标地址
- [ ] 添加 → 待验证 → 「我已在 CF 确认」→ 已验证
- [ ] 首个验证地址自动成为 default_forward（设置页可见）
- [ ] 被规则引用的地址删除 → 409 提示
- [ ] 🌐 生产：CF 发送确认邮件 → 点击后面板自动变已验证（CF API 模式）

### B5 域名（§3.4）
- [ ] 手动登记域名 + 标记 Email Routing 状态（绿点/灰点）
- [ ] 侧栏分区 ER ×n 徽章、点击域名跳转过滤邮件
- [ ] 🌐 配置真实 CF API Token（Zone.Read + Email Routing Read + DNS Edit）→ 「从 Cloudflare 同步」拉取全部 zone
- [ ] 🌐 「开启转发」：MX 冲突检测 → 确认 → DNS 自动配置 → catch-all → Worker
- [ ] 🌐 「导入原生规则」：CF 原有规则转为 InboxFly 规则并删除原生规则（发测试信验证不再绕过）

### B6 统计
- [ ] 六张汇总卡（处理状态口径 + 累计流量）
- [ ] 14 日堆叠柱状图（悬停看明细）
- [ ] 按域名分布

### B7 设置
- [ ] 默认动作三种切换（配合模拟收信观察无规则命中时的行为）
- [ ] blocked_mail_action drop/reject
- [ ] 存储模式切「仅元数据」→ 新模拟邮件只存元数据
- [ ] R2 保留天数修改 → 手动清理按新值执行
- [ ] 管理员：改用户名/密码（错误当前密码 → 403；成功后其他端会话失效）
- [ ] Turnstile 开关即时生效（关掉后登录页无组件）
- [ ] CF API Token 保存/清除

### B8 外观与国际化
- [ ] 三主题 × 浅色/深色（6 组合）逐一检查对比度
- [ ] 深浅色跟随系统 / 手动切换持久化
- [ ] 10 种语言逐一切换抽查（重点：日/韩/德/俄长文案不破版）
- [ ] 侧边栏 ⇄ 页眉布局切换 + 侧栏折叠（64px 图标栏）
- [ ] 移动端（DevTools 390×844）：抽屉导航、单列布局、批量操作

### B9 PWA
- [ ] DevTools → Application → Manifest 无报错、图标齐全
- [ ] Service Worker 注册成功（Application → Service Workers）
- [ ] 断网刷新：静态外壳可离线渲染（API 提示失败属预期）
- [ ] 🌐 生产 HTTPS 下：地址栏安装图标 → 安装到桌面/手机主屏

## C. 需要真实环境（本地无法覆盖）

| # | 项目 | 方法 | 关注点 |
|---|---|---|---|
| 🌐1 | 真实收发信全链路 | wrangler deploy + Email Routing + 给域名发真实邮件 | forward() 实际投递、R2 副本落地、统计准确 |
| 🌐2 | 送达率 / 垃圾箱率（M0 spike） | 向 Gmail/QQ/Outlook 各发若干封 | 是否进垃圾箱；README 收件侧白名单指引是否够用 |
| 🌐3 | CF API Token 写路径联调 | 创建真实 Token（Zone.Read + Email Routing Addresses/Rules Edit + DNS Edit） | 一键开启转发、CF 目标地址添加/回同步、原生规则导入 |
| 🌐4 | Cron 清理 | 生产部署后等 03:00 UTC 或临时改 cron 表达式 | 过期对象被删除、统计不受影响 |
| 🌐5 | Turnstile 真实密钥 | CF Dashboard 创建站点 | 真实验证通过、失败被拒 |
| 🌐6 | Deploy 按钮 | 推送 GitHub 仓库后点击按钮部署 | 一键部署流程、Secret 配置引导 |
| 🌐7 | workers.dev 可达性 | 部署后在目标网络环境访问 | 是否需要为面板绑定自定义域 |

## D. 已知边界（测试时不要误报）

- 本地「模拟收信」不走真实投递（本地无 Email Routing），per_destination 标记 `simulated: true`
- 「已转发」= CF 受理转发，最终投递（含对方垃圾箱）不可观测（§7.1）
- 目标地址「我已在 CF 确认」为手动模式自申报；CF API 模式下状态自动回同步
- KV 规则缓存未启用（直读 D1）；Turnstile 密钥错误时登录报 `turnstile_failed`
