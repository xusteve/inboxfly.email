{
  "name": "inboxfly",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "InboxFly — 开源的 Cloudflare 邮件转发统一管理工具（自部署版）",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute inboxfly --local --file schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "postal-mime": "^2.3.2"
  },
  "devDependencies": {
    "wrangler": "^4.16.0"
  }
}
